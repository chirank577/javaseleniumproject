package feb21stobjectcreation;

public class MethodDeclaration {
    // method declaration is nothing but creating a method

    //syntax of declaring a method:
    //access modifier return type method name(args to be passed if any)

    // access modifier. defines the scope or visibility of the method
    //public --> method is accessible anywhere within the project
    //private--> method is accessible within that class only
    //default--> method is accessible within that package only
    //protected--> method is accessible within that package only, accessible outsideof the package via inheritance

    //returntype--> output that we are expeting from the method
    // it can be int, boolean any primitive type along with void
    //void means executes the lines of the code present inside the method
    public static void main(String[] args) {

    }
}
